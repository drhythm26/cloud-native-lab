apiVersion: v1
kind: Service
metadata:
  name: go-api
  namespace: go-api
  labels:
    app: go-api
spec:
  selector:
    app: go-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
