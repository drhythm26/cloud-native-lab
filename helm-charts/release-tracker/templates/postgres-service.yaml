apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: release-tracker
  labels:
    {{- include "release-tracker.labels" . | nindent 4 }}
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
