services:
  go-api:
    container_name: go-api
    image: go-api:0.2.0
    ports:
      - "8080:8080"
    environment:
      - APP_ENV=lab
      - LOG_LEVEL=debug
      - API_TOKEN=fake
    volumes:
      - ./config/:/etc/go-api/
