apiVersion: v1
kind: ConfigMap
metadata:
  name: release-tracker-config
  namespace: release-tracker
data:
  DB_HOST: postgres
  DB_PORT: "5432"
  DB_NAME: release-tracker
  DB_USER: postgres
  DB_SSLMODE: disable
